# 🚀 Инструкция по загрузке проекта на GitHub

## ✅ Проект готов к загрузке!

Git репозиторий инициализирован, все файлы добавлены и закоммичены.

---

## 📋 Шаги для загрузки на GitHub:

### 1. Создайте репозиторий на GitHub

1. Зайдите на **https://github.com/**
2. Нажмите кнопку **"+"** в правом верхнем углу
3. Выберите **"New repository"**
4. Заполните форму:
   - **Repository name:** `portfolio-website` (или любое другое название)
   - **Description:** `Personal portfolio website with multi-language support`
   - Выберите **Public** или **Private**
   - **ВАЖНО:** НЕ добавляйте README, .gitignore или лицензию (они уже есть в проекте)
5. Нажмите **"Create repository"**

### 2. Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет инструкции. Выполните одну из команд:

**Если используете HTTPS:**
```bash
git remote add origin https://github.com/ВАШ_USERNAME/НАЗВАНИЕ_РЕПОЗИТОРИЯ.git
git branch -M main
git push -u origin main
```

**Если используете SSH:**
```bash
git remote add origin git@github.com:ВАШ_USERNAME/НАЗВАНИЕ_РЕПОЗИТОРИЯ.git
git branch -M main
git push -u origin main
```

**Замените:**
- `ВАШ_USERNAME` - ваш GitHub username
- `НАЗВАНИЕ_РЕПОЗИТОРИЯ` - название репозитория, которое вы создали

### 3. Введите учетные данные

При первом push GitHub попросит ввести:
- **Username:** ваш GitHub username
- **Password:** используйте Personal Access Token (не пароль!)

**Как создать Personal Access Token:**
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите scope: `repo` (полный доступ к репозиториям)
4. Скопируйте токен и используйте его как пароль

---

## 🎯 После загрузки на GitHub:

### Настройка автоматического деплоя (опционально)

#### Вариант 1: GitHub Pages

1. Зайдите в репозиторий на GitHub
2. **Settings** → **Pages**
3. В разделе **"Source"** выберите **"GitHub Actions"**
4. При следующем push в main ветку сайт автоматически задеплоится
5. Сайт будет доступен по адресу: `https://ВАШ_USERNAME.github.io/НАЗВАНИЕ_РЕПОЗИТОРИЯ/`

#### Вариант 2: Vercel (рекомендуется)

1. Зайдите на **https://vercel.com/**
2. Войдите через GitHub
3. Нажмите **"Add New Project"**
4. Выберите ваш репозиторий
5. Vercel автоматически определит Astro проект
6. Нажмите **"Deploy"**
7. Сайт будет доступен по адресу: `https://ваш-проект.vercel.app`

#### Вариант 3: Netlify

1. Зайдите на **https://app.netlify.com/**
2. Войдите через GitHub
3. Нажмите **"Add new site"** → **"Import an existing project"**
4. Выберите ваш репозиторий
5. Настройки:
   - Build command: `npm run build`
   - Publish directory: `dist`
6. Нажмите **"Deploy site"**

---

## ✅ Чеклист перед загрузкой:

- ✅ Git репозиторий инициализирован
- ✅ Все файлы добавлены в git
- ✅ Первый коммит создан
- ✅ `.gitignore` настроен правильно (исключает `node_modules/`, `dist/`, `.env`)
- ✅ `README.md` обновлен
- ✅ Инструкции по деплою созданы

---

## 📝 Полезные команды Git:

```bash
# Проверить статус
git status

# Посмотреть историю коммитов
git log --oneline

# Добавить изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push

# Получить изменения с GitHub
git pull
```

---

## 🔒 Безопасность:

Убедитесь, что в репозиторий НЕ попали:
- ❌ Файлы с паролями или API ключами
- ❌ `node_modules/` (уже в .gitignore)
- ❌ `dist/` (уже в .gitignore)
- ❌ `.env` файлы (уже в .gitignore)

Все чувствительные данные должны быть в `.gitignore`!

---

## 🎉 Готово!

После выполнения этих шагов ваш проект будет на GitHub и готов к деплою!

Если возникнут вопросы, см. файл `QUICK_DEPLOY.md` для инструкций по деплою.
