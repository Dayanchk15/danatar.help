# 🚀 Инструкции по деплою проекта

Проект успешно собран! Файлы готовы в папке `dist/`.

## Быстрый деплой (рекомендуется)

### Вариант 1: Cloudflare Pages (Бесплатно, быстро)

1. **Зайдите на** https://dash.cloudflare.com/
2. **Войдите** в аккаунт или создайте новый
3. **Выберите** "Pages" в меню слева
4. **Нажмите** "Create a project"
5. **Выберите** "Upload assets"
6. **Перетащите** всю папку `dist/` в область загрузки
7. **Введите** название проекта (например: `viio`)
8. **Нажмите** "Deploy site"
9. **Готово!** Ваш сайт будет доступен по адресу `https://ваш-проект.pages.dev`

### Вариант 2: Vercel (Бесплатно, очень просто)

1. **Зайдите на** https://vercel.com/
2. **Войдите** через GitHub/GitLab/Bitbucket или email
3. **Нажмите** "Add New Project"
4. **Если проект в Git:**
   - Подключите репозиторий
   - Vercel автоматически определит Astro
   - Нажмите "Deploy"
5. **Если проекта нет в Git:**
   - Установите Vercel CLI: `npm install -g vercel`
   - Выполните: `vercel --prod` в папке проекта

### Вариант 3: Netlify (Бесплатно)

1. **Зайдите на** https://app.netlify.com/
2. **Войдите** в аккаунт
3. **Перетащите** папку `dist/` в область "Deploy manually"
4. **Готово!** Сайт будет доступен по адресу `https://случайное-имя.netlify.app`

## Деплой через Git (автоматический)

Если ваш проект в Git репозитории, можно настроить автоматический деплой:

### Cloudflare Pages + Git:
1. В Cloudflare Pages выберите "Connect to Git"
2. Подключите ваш репозиторий
3. Настройки:
   - **Build command:** `npm run build`
   - **Build output directory:** `dist`
   - **Root directory:** `/`
4. Каждый push в main ветку будет автоматически деплоить сайт

### Vercel + Git:
1. В Vercel выберите "Import Git Repository"
2. Подключите репозиторий
3. Vercel автоматически определит настройки из `vercel.json`
4. Нажмите "Deploy"

## Текущий статус

✅ Проект собран успешно  
✅ Файлы готовы в папке `dist/`  
✅ Конфигурационные файлы созданы:
   - `vercel.json` - для Vercel
   - `netlify.toml` - для Netlify

## Рекомендация

**Cloudflare Pages** - самый быстрый и простой вариант:
- Бесплатный
- Быстрый CDN
- Автоматический HTTPS
- Простой интерфейс
- Просто перетащите папку `dist/`

---

**Важно:** После деплоя проверьте, что все страницы работают:
- `/` - главная (редирект на `/ru/`)
- `/ru/` - русская версия
- `/en/` - английская версия  
- `/tk/` - туркменская версия
